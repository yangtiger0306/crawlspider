import * as browser from './browser';
import * as db from './db';

export let getBrowser = browser.getBrowser;
export let getPage = browser.getPage;

export let getDb = db.getDb; 
export let closeDb = db.closeDb;